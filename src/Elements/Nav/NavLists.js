import styled from "styled-components";

export const NavList = styled.ul`
  list-style: none;
  margin: 0;
  padding: 0;
`;

export const NavbarList = styled(NavList)`
  font-size: 14px;
  font-weight: 600;
  text-transform: uppercase;
`;
